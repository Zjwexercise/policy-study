# 考研政治刷题掌上宝 - 云服务器极简部署指南 🚀

不管你购买的是**阿里云、腾讯云、华为云**的轻量服务器（推荐 Ubuntu 22.04 / 24.04 或 CentOS / Debian），只需以下简单几步，即可彻底摆脱电脑限制，在自习室、床上用手机随时随地刷题！

---

## 方式 1：Docker 一键部署（最推荐，零依赖烦恼）

### 第 1 步：将项目代码上传到云服务器
在本地电脑的项目文件夹 `policy/` 下，把代码打包成 zip 上传到云服务器，或者用 scp 上传：
```bash
# 在云服务器上解压后进入目录
cd policy
```

### 第 2 步：运行一键脚本
```bash
bash deploy.sh
```
脚本会自动安装 Docker、构建镜像并在后台 24 小时运行，数据库和做题记录已通过数据卷持久化，服务器重启也不会丢失！

---

## 方式 2：纯 Python 后台运行（最轻量，不用 Docker）

如果服务器配置较低（如 1核1G / 1核2G），可以直接使用 Python 运行：

### 第 1 步：安装 Python 环境依赖
```bash
sudo apt update && sudo apt install -y python3 python3-pip
cd policy/backend
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### 第 2 步：后台常驻运行
```bash
nohup python3 main.py > server.log 2>&1 &
```

服务即在后台永续运行，可以通过 `ps -ef | grep main.py` 查看。

---

## ⚠️ 极其关键的一步：云平台防火墙放行

不管用哪种方式部署，**必须在云服务器控制台的安全组/防火墙中放行 `8000` 端口**：
1. 登录阿里云/腾讯云控制台。
2. 找到你的服务器 -> 点击【安全组】或【防火墙】。
3. 添加规则：
   - **端口**：`8000`
   - **协议**：`TCP`
   - **源地址**：`0.0.0.0/0`（允许所有网络访问）
4. 保存后，手机或电脑直接在浏览器打开：
   `http://你的服务器公网IP:8000`

---

## 📱 手机端使用高分技巧
1. 在手机 Safari（苹果）或 Chrome/夸克/Edge（安卓）打开 `http://你的服务器IP:8000`。
2. 点击浏览器的 **【分享】** 或 **【菜单】** -> 选择 **【添加到主屏幕】**。
3. 手机桌面上会生成一个名为“考研政治刷题”的独立 App 图标，点击直接全屏打开，无浏览器地址栏，沉浸感拉满！
